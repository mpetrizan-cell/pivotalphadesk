[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn gaia_server:app --bind 0.0.0.0:$PORT --workers 1 --threads 2"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
